export default interface IMenuItem {
    id: string;
    name: string;
    imageUrl: string;
    categoryName: string
    description: string;
    ratingValue: number;
    reviewsCount: number;
    price: number;
}